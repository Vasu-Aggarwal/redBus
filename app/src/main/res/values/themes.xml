<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RedBus" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/splash_red</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="animationDialog">
        <item name="enterAnim">@anim/zoom_in</item>
        <item name="exitAnim">@anim/zoom_out</item>
    </style>

    <style name="BottomNavigationText" parent="@android:style/TextAppearance"/>
    <style name="BottomNavigationText.Selected">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">14sp</item>
    </style>
    <style name="BottomNavigationText.unSelected">
        <item name="android:textSize">13sp</item>
    </style>

</resources>